# Session Configuration
SESSION_KEY=tuclavesegura
SECURE=false

DATABASE_URL=postgres://user:password@host:5432/dbname

# Database Configuration (Option 2: Individual Variables - used if DATABASE_URL is not set)
DB_USER=user
DB_PASSWORD=password
DB_NAME=db
DB_HOST=host
